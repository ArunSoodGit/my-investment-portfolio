apiVersion: v1
kind: Service
metadata:
  name: api-server
spec:
  selector:
    app: api-server
  ports:
    - protocol: TCP
      port: 8080      # port w Kubernetes
      targetPort: 8080 # port w kontenerze
      nodePort: 30080  # port dostępny z hosta
  type: NodePort
