apiVersion: v1
kind: Service
metadata:
  name: reservation-service
spec:
  selector:
    app: reservation-service
  ports:
    - protocol: TCP
      port: 8081      # port w Kubernetes
      targetPort: 8081 # port w kontenerze
      nodePort: 30081  # port dostępny z hosta
  type: NodePort
